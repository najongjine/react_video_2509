import { useEffect, useState } from "react";

export default function Home() {
  /* 우리가 만든 api 에서 비디오 리스트 가져와서 화면에 뿌리기 */

  async function getVideoList() {
    //...
  }

  useEffect(() => {}, []);

  return (
    <div className="content-margin-padding">
      <h1>비디오 리스트</h1>
    </div>
  );
}
